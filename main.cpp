#include <iostream>
#include "ProcessQueue.h"

int main() {
//    ProcessQueue * q = new ProcessQueue(nullptr, nullptr);
//    q -> enqueue(new Process(1, 2, 3, std::chrono::system_clock::now(), std::chrono::system_clock::now(), 4, 5));
//    q -> enqueue(new Process(5, 4, 3, std::chrono::system_clock::now(), std::chrono::system_clock::now(), 2, 1));
//    q -> enqueue(new Process(6, 7, 8, std::chrono::system_clock::now(), std::chrono::system_clock::now(), 9, 10));
//    int value = q -> getFront() -> getData() -> getProcessId();
//    std::cout << value << std::endl;
//    q -> dequeue();
//    value = q -> getFront() -> getData() -> getProcessId();
//    std::cout << value << std::endl;
//    q -> dequeue();
//    value = q -> getFront() -> getData() -> getProcessId();
//    std::cout << value << std::endl;

    return 0;
}